# 答辩演示故事线

## 故事一：AI应用开发路径闭环

使用 `USER-G002`。先展示后端与 API 优势，再展示 RAG、模型评估和监控差距。完成课程与项目后刷新雷达图和岗位匹配分，最后比较主路线与算法备选路线。

演示重点：能力证据、动态画像、任务推荐、匹配解释和双分支路径。

## 故事二：测试工程新人转向AI应用

使用 `USER-G009`。初始优势是测试、沟通和协作，目标能力缺口是后端、容器与部署。完成针对性项目后，AI应用开发工程师成为匹配第一。

演示重点：`USER-G009` 修正后的目标岗位排序、技能映射和可验证项目成果。

## 故事三：旧技能衰减与重新验证

使用 `USER-G012`。该用户希望转向算法岗位，但旧有机器学习证据衰减，当前算法岗排第二。系统不强行迎合目标，而是给出证据刷新任务和数据岗备选路线。

演示重点：技能衰减、解释性匹配、不确定性表达和动态路径调整。

## 故事四：隐私撤回

继续使用 `USER-G012` 的 `consent_withdrawn` 事件。触发后停止画像更新，进入删除流程测试，并通过隐私与越权评测案例验证系统不会继续处理或外发数据。

演示重点：最小必要、同意可撤回、MCP 工具权限和安全拒绝。
